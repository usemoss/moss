/**
 * Filter passed to `workbench.action.openSettings` for this extension.
 * Must stay aligned with `publisher` + `name` in package.json (`inferedge` + `vscode-moss`).
 */
export const MOSS_EXTENSION_SETTINGS_QUERY = "@ext:inferedge.vscode-moss";
