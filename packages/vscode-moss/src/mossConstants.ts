/**
 * Filter passed to `workbench.action.openSettings` for this extension.
 * Must stay aligned with `publisher` + `name` in package.json (`moss-dev` + `vscode-moss`).
 */
export const MOSS_EXTENSION_SETTINGS_QUERY = "@ext:moss-dev.vscode-moss";
