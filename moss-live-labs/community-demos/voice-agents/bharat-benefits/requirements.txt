# Core
moss>=1.0.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Audio recording and playback
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0

# Audio playback (optional - agent still works without it, saves wav to disk)
simpleaudio>=1.0.4

# Voice activity detection (optional - only needed for --vad mode)
webrtcvad>=2.0.10