[project]
name = "moss-cross-agent-handoff"
version = "0.1.0"
description = "Cross-agent handoff (shared session), built on Moss"
requires-python = ">=3.10"
dependencies = [
    "moss>=1.7.1",
    "python-dotenv>=1.0",
]
