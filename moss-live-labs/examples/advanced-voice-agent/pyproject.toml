[project]
name = "advanced-voice-agent"
version = "0.1.0"
description = "Advanced voice agent example using Moss"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.0.0",
    "moss>=1.0.1",
    "livekit-agents[openai,deepgram,silero,turn-detector,cartesia,google,inference]>=1.0",
]
